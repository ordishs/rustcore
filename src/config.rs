use crate::stdlog as _stdlog;
use crate::SHOW_STD_TIMESTAMP as _SHOW_STD_TIMESTAMP;

// Stub — full implementation added in subsequent tasks.
// References to crate-level items prevent dead_code warnings during incremental builds.
const _: () = {
    let _ = _stdlog;
    let _ = &_SHOW_STD_TIMESTAMP;
};
